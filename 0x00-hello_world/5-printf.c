#include <stdio.h>


/**
 * main - the entry point into the program.
 * this program demostrates the use of the printf function.
 * Return: the program returns an integer 0, for success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
